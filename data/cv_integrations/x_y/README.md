# CV-интеграция x_y

Активный вариант интеграции, где CV-target содержит только позицию аппарата.
Изображения и метки не дублируются: metadata указывает на общие
`data/images/` и `data/labels.csv`.

Целевые колонки:

- `x`
- `y`
