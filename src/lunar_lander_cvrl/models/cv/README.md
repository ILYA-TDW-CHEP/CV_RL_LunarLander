# Версии CV-моделей

Размещайте здесь будущие реализации computer-vision моделей.

Текущий ResNet18-регрессор позы остаётся в `lunar_lander_cvrl.vision` для
обратной совместимости.

`train_cv.py` сейчас поддерживает:

- `resnet18`
- `simple-cnn`
